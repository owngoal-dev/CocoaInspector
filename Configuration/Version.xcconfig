// Single source of truth for the app, daemon, CLI, and Debian package version.
// Update with `make set-version VERSION=1.2.3`; CI applies the vX.Y.Z tag here.

MARKETING_VERSION = 0.5.6
CURRENT_PROJECT_VERSION = 12
